{% extends 'base.html.twig' %}

{% block title %}ListaProductos{% endblock %}

{% block body %}
    <h1>ListaProductos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lista_producto.id }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ lista_producto.cantidad }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lista_productos_index') }}">back to list</a>

    <a href="{{ path('app_lista_productos_edit', {'id': lista_producto.id}) }}">edit</a>

    {{ include('lista_productos/_delete_form.html.twig') }}
{% endblock %}
